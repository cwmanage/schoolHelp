# 课屿（schoolHelp）需求文档

> 版本：v3（课表智能化 + AI 助教 + 校园日历）｜ 品牌名：**课屿**（仓库名/包名沿用 schoolHelp） ｜ 定位：哈尔滨学院专属校园助手 ｜ 性质：研一练手、校内小范围使用
> 关联文档：`README.md`、`开发日志.md`、`代码健壮性走查报告.md`
> 版本历史：v1 基础功能 → v2 审批流 → **v3 课表智能化 / AI 助教（免费大模型）/ 校园日历 / 七天免登录 / 仓库脱敏**

---

## 一、项目背景与目标

### 背景

同学日常信息分散在多个群里：课表在教务系统、作业在老师板书/群消息、资料在 QQ 群文件、吐槽在匿名墙。信息不统一，容易漏作业、找不到资料。

### 目标

做一个**统一、可控、可追溯**的校园信息小工具，覆盖"课表 / 课程 / 作业 / 资料 / 评论"五个高频场景，做到：

1. 信息集中，一处维护、多处可见；
2. 关键提醒（临期作业）主动触达；
3. 用户产生的内容**先审后展示**，管理员可管、可删、可追溯；
4. 移动端 + PC 端都能用。

### 非目标（明确不做 / 暂不做）

- ❌ 不做教务系统对接（不爬数据、不碰成绩/学分）；
- ❌ 不做付费、不做返佣、不做面向大众的商业化；
- ❌ 不做"代刷课/代挂"等违规功能。

> v3 变更：原「第一版不做课表 OCR」已交付 —— v3 通过免费大模型视觉接口实现课表截图识别导入（见 FR-10）。

---

## 二、用户角色与权限

| 角色 | 值 | 能力 |
|------|-----|------|
| 同学 | 0 | 浏览已通过内容；申请创建课程/作业/资料（走审批）；管理自己的课表、资料；发评论（可匿名）；标记作业已提交 |
| 班长 | 1 | 同学全部能力 + **录入课程/作业/资料直接通过（免审）** |
| 管理员 | 2 | 全部能力 + **审批**（通过/驳回，驳回必填原因）+ 删除任意内容 + 查看匿名评论真实身份 + 下架课程 |

> 说明：班长身份不开放自助注册，需管理员在数据库把 `user.role` 置为 1。

---

## 三、功能需求

### FR-1 账号与认证

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-1.1 | 注册 | 用户名 + 密码 + 确认密码 + 角色；用户名唯一 |
| FR-1.2 | 登录 | 用户名 + 密码 → 签发 JWT |
| FR-1.3 | 密码强度 | 前后端一致：**最少 8 位、最多 32 位，且必须同时含字母和数字**（后端 `@Size`+`@Pattern` 强校验，前端同步） |
| FR-1.4 | 密码加密 | BCrypt 存储，不可逆 |
| FR-1.5 | 鉴权 | 除白名单（注册/登录等）外，所有接口需携带有效 JWT |
| FR-1.6 | 个人资料 | 可改昵称、院系、班级、签名、头像 |
| FR-1.7 | 修改密码 | 需校验原密码 |
| FR-1.8 | 默认管理员 | 首次启动自动创建 `admin/******`，**提示尽快改密**（密码常量拆分存储，仓库不出现明文） |
| FR-1.9 | 七天免登录 | JWT 有效期 7 天；网关校验通过后若剩余有效期 < 3 天自动重签新 token（响应头 `X-Renewed-Token`），前端拦截器静默更新本地存储。效果：**7 天内有任意一次访问即保持登录**，连续 7 天不访问才需重新登录 |

### FR-2 个人课表

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-2.1 | 周视图课表 | 移动端支付宝风格周视图；按星期 × 节次排布；连堂课按节次跨度合并展示（跨行高度） |
| FR-2.2 | 录入课程 | 可手动录入：课程名、星期、起止节次、周类型（每周/单周/双周）、周次、教室、教师、学期 |
| FR-2.3 | 关联课程库 | 录入时可选关联课程库中的课程（带 `courseId`），自动带入常用教室；也可纯自录 |
| FR-2.4 | 编辑/删除 | 可编辑、删除自己的课表条目；同课多时段（同名同学期）时可选「仅此时段 / 该课全部时段」联动（联动只同步课程名/教师/教室/单双周/周次，各时段时间保留） |
| FR-2.5 | 点击行为 | 点击课程统一弹**详情弹窗**（时间/单双周/周次/教室/教师/学期/时段聚合/课程库状态），弹窗内提供：编辑课程、申请录入课程库、课程详情跳转（已关联或已入库同名同教师课） |
| FR-2.6 | 同课多时段同色 | 同一门课的多个时段按课程名哈希取同一颜色；批量入库申请按课程名去重 |
| FR-2.7 | 作息配置 | 每用户一套每节课起止时间（`schedule_time_config`，默认 12 节大学作息模板）；可设每天节数（4-12）、每节时长（默认 45 分钟）、课间间隔（默认 10 分钟）；**修改上课时间自动计算下课时间**，后续节次按课间间隔顺延；时间列实时显示起止时间 |
| FR-2.8 | AI 作息建议 | 设置弹窗按每天节数调用大模型生成作息建议（非法自动回退默认模板），用户微调后保存 |
| FR-2.9 | 周翻页与周数 | 设置「学期第一周开始日期」（每用户每学期一份）后：表头显示日期、顶部显示「第 N 周」、支持 ←/→ 翻页查看其他周课程；**非视图周的课程虚化显示**（周次文本 + 单双周过滤）；未设置日期时不虚化、仅提示设置 |
| FR-2.10 | 拖拽调课 | PC 端课程块可拖拽到新格子，弹窗选择：**临时调课（仅本周生效，下周自动恢复，存 `schedule_change`）** 或 **永久调课（本学期该时段变更）**；临时调课的块显示标记，可恢复原时间 |
| FR-2.11 | 设备自动分流 | 手机访问 PC 站自动跳移动端（`?pc=1` 强制留在 PC），反之亦然 |

### FR-3 课程库（含审批）

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-3.1 | 浏览课程 | 展示所有 `status=1`（已通过）课程，可按班级/学期筛选 |
| FR-3.2 | 课程详情 | 展示课程名、教师、教师主页链接、班级、学期、简介 |
| FR-3.3 | 申请创建 | 全员可申请；班长/管理员直接通过，同学进入待审批 |
| FR-3.4 | 修改 | 管理员/班长可改；**同学改自己已通过内容 → 打回待审批**（防绕过） |
| FR-3.5 | 删除 | 管理员任意删；同学只能删自己未通过的申请 |
| FR-3.6 | 我的申请 | 申请人可查看自己的全部申请及状态 |
| FR-3.7 | 审批 | 管理员对待审课程通过/驳回；驳回必填原因 |
| FR-3.8 | 下架 | 管理员可将课程置为 `status=3` 下架 |

### FR-4 作业（含审批）

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-4.1 | 作业列表 | 按课程展示已通过作业，含标题、内容、截止时间 |
| FR-4.2 | 发布作业 | 需选择已通过课程；班长/管理员直接通过，同学待审批 |
| FR-4.3 | 修改/删除 | 同课程规则（同学改已通过内容打回待审批） |
| FR-4.4 | 标记已提交 | 用户点"已提交"后不再滚动提醒；可取消；**校验作业存在** |
| FR-4.5 | 倒计时 | 展示距截止的剩余时间 / 天数 |

### FR-5 临期作业提醒

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-5.1 | 聚合规则 | **我的课表（有关联课程）∩ 作业截止 ≤ 5 天 ∩ 我未提交** |
| FR-5.2 | 展示 | 首页红色条幅滚动提示，按截止时间升序（最急在前） |
| FR-5.3 | 点击 | 点击跳转对应课程/作业 |

### FR-6 课程资料（含审批）

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-6.1 | 资料列表 | 按课程展示已通过资料，可下载 |
| FR-6.2 | 上传 | 需选择已通过课程；单文件 ≤ 200MB；班长/管理员直接通过，同学待审批 |
| FR-6.3 | 申请说明 | 同学上传时附带申请说明（`applyNote`），供管理员审批参考 |
| FR-6.4 | 删除 | 管理员任意删（同时删文件）；上传人删自己未通过的申请 |
| FR-6.5 | 存储 | 文件存服务器本地/NAS 目录，数据库仅存路径 |

### FR-7 课程评论

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-7.1 | 发表评论 | 登录用户可对**已通过**课程发表评论；内容非空、≤500 字 |
| FR-7.2 | 匿名 | 可选匿名；匿名对同学显示"匿名同学 + 黑客头像"，且接口不返回真实 `userId` |
| FR-7.3 | 管理员追溯 | 管理员可通过专用接口查看匿名评论的真实 `user_id` |
| FR-7.4 | 删除 | 管理员任意删；用户删自己的评论（逻辑删除，保留可追溯） |

### FR-8 天气与穿衣推荐

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-8.1 | 天气展示 | 双端展示哈尔滨学院天气（当前 + 未来 3 天） |
| FR-8.2 | 数据源 | Open-Meteo 免费 API（无需 key） |
| FR-8.3 | 穿衣推荐 | 按体感温度分层 + 天气码 + 风速修正 |
| FR-8.4 | 弹窗策略 | 会话内只弹一次；同一天"今日不再提示" |
| FR-8.5 | 入口 | 移动端课表页天气按钮 + 自动弹；PC 首页日期右侧按钮 |

### FR-9 审批中台（管理员）

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-9.1 | 待审列表 | 汇总三类待审（课程/作业/资料） |
| FR-9.2 | 审批操作 | 通过 / 驳回（驳回必填原因） |
| FR-9.3 | 查看申请信息 | 展示申请人、申请说明 |

### FR-10 AI 助教（免费大模型）

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-10.1 | 模型接入 | 智谱开放平台 **GLM-4-Flash（文本）+ GLM-4V-Flash（视觉）**，均为免费模型；OpenAI 兼容接口，服务端代理转发（key 用 Jasypt `ENC(...)` 加密存储，仓库不落明文） |
| FR-10.2 | 文字问答 | 两端右下角浮动按钮 + 对话抽屉（登录后可用）；多轮上下文（携带最近 6 条）；历史存浏览器本地按用户区分；**assistant 回复按 Markdown 渲染**（防 XSS），发送后有加载动画；每用户限流 5 次/分钟 |
| FR-10.3 | 课表截图识别 | 课表页「截图导入」上传课表截图（≤5MB）→ 视觉模型识别 → 返回结构化课程条目（课程名/星期/节次/教室/教师/单双周/周次）→ **用户逐条核对修改后确认导入**（AI 结果不直接入库） |
| FR-10.4 | 批量申请入库 | 导入预览页可开启「同时申请录入课程库」，逐条勾选；同名课程只申请一次；导入完成分别汇报课表成功数与申请数 |
| FR-10.5 | 提示词约束 | 服务端注入 system prompt（校园学习助手身份、中文简洁、拒答敏感内容） |

### FR-11 校园日历（考试 / 竞赛 / 节假日 / 活动）

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-11.1 | 数据来源 | 服务端**每日定时任务**（03:10 + 启动时）同步：① 节假日调免费 API（国务院公布数据）；② 考试/竞赛调用大模型生成当年日历（失败回退内置知识库）；所有 AI 生成内容标注「以官方通知为准」 |
| FR-11.2 | 事件存储 | `campus_calendar_event`（类型/标题/单日或区间日期/时间说明/来源），内容变化才更新版本号 |
| FR-11.3 | 更新提示 | 用户登录进入首页时检查是否有未读日历更新，有则弹提示「您的日历有新大学活动更新，请及时关注」并标记已读 |
| FR-11.4 | 课表展示 | 课表网格底部新增「大学活动」行：各天列显示当日事件（类型色点 + 标题 + 时间说明）；PC 首页另有「本周校园日历」卡片；事件随周翻页加载对应周数据 |

---

## 四、非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 校园小规模（百人级）够用即可；临期聚合按课程串行拉取，课程多时可改批量接口 |
| 安全 | JWT 鉴权、BCrypt 密码、网关统一校验、服务间直连 Nacos 不经公网；数据库/Nacos 密码等敏感配置用 Jasypt 加密存储（`ENC(...)`） |
| 可维护 | 公共逻辑收敛在 common；单表 CRUD 用 MyBatis-Plus；配置外置 |
| 可追溯 | 审批记录（申请人/说明/意见/时间）、评论真实 user_id、逻辑删除 |
| 兼容 | 移动端 + PC 端双套前端；主流现代浏览器 |
| 合规 | 面向校内小范围，先审后展示，具备"通知—删除"能力 |

---

## 五、审批状态机（统一约定）

```
                    ┌──────────────┐
   提交申请 ───────>│ 0 待审批      │
 （普通同学）        └──────┬───────┘
                          │ 管理员审批
              ┌───────────┴───────────┐
        通过  ▼                        ▼  驳回（必填原因）
   ┌──────────────┐            ┌──────────────┐
   │ 1 已通过      │            │ 2 已驳回      │
   │（公开可见）    │            │（可改后重提）  │──> 修改 ──> 回到 0
   └──────────────┘            └──────────────┘
                                  课程另有：
                              ┌──────────────┐
                              │ 3 下架(offline)│  （管理员手动）
                              └──────────────┘
```

**免审直通**：班长 / 管理员提交的内容直接置为 `1 已通过`。

---

## 六、数据模型（12 张表）

| 表 | 说明 | 关键字段 |
|----|------|----------|
| `user` | 用户 | username(唯一)、password(BCrypt)、role(0/1/2)、school_id、status |
| `course` | 课程库 | name、teacher_name、teacher_link、class_name、**room(常用教室)**、semester、creator_id、apply_user_id、apply_note、review_note、reviewed_at、status(0/1/2/3) |
| `schedule` | 个人课表 | user_id、course_id(可空)、course_name、week_day、start_section、end_section、week_type、weeks、room、teacher、semester |
| `course_material` | 课程资料 | course_id、title、file_name、file_path、file_size、file_type、uploader_id、status、apply_user_id、apply_note、review_note、reviewed_at |
| `assignment` | 作业 | course_id、title、content、deadline、creator_id、apply_user_id、apply_note、review_note、reviewed_at、status(0/1/2) |
| `assignment_submit` | 作业提交标记 | assignment_id + user_id（唯一），submit_time |
| `course_comment` | 课程评论 | course_id、user_id(真实)、content、is_anonymous、status(1正常/0删) |
| `schedule_time_config` | 作息配置（v4） | user_id + section（唯一）、start_time、end_time（HH:mm） |
| `schedule_semester_setting` | 学期设置（v5） | user_id + semester（唯一）、week1_date（第一周周一） |
| `campus_calendar_event` | 校园日历事件（v5） | event_type(exam/contest/holiday/activity)、title、event_date 或 date_start/date_end、time_note、data_version |
| `user_calendar_read` | 日历已读（v5） | user_id（主键）、last_read_at |
| `schedule_change` | 调课记录（v5） | user_id、schedule_id、week_start（生效周）、week_day、start_section、end_section |

**说明**：
- `assignment` 表**无 `updated_at`**（与 course 不同），实体与表一致，非缺陷；如需审计时间需加字段。
- 审批字段由 `v2_approval_migration.sql` 增量添加，存量数据回填为"已通过"。
- v3 新增迁移：`v3_user_approve_migration.sql`（班长审批）、`v4_schedule_time_config.sql`（作息）、`v5_schedule_upgrade.sql`（课表升级：学期设置/日历/调课/课程库教室列），全部幂等可重复执行。

---

## 七、接口约定

- **统一入口**：网关 `http://<host>:8080`，所有 API 前缀 `/api`，经 `StripPrefix` 去掉后转发。
- **统一响应**：`{ code, message, data }`，`code=200` 为成功，其余为失败（含业务码 400/403/404/500）。
- **鉴权头**：`Authorization: Bearer <token>`。
- **主要路由前缀**：

| 服务 | 前缀 | 示例 |
|------|------|------|
| user | `/api/user` | `POST /api/user/auth/login`、`GET /api/user/schedule/list-by-user` |
| course | `/api/course` | `GET /api/course/course/list`、`POST /api/course/assignment`、`POST /api/course/material/course/{id}` |
| biz | `/api/biz` | 临期作业、评论、提交标记 |

---

## 八、验收标准（核心场景）

1. 新同学注册 → 登录 → 进入课表页，可看天气、录课。
2. 同学申请创建课程 → 公开列表**看不到** → "我的申请"可见 → 管理员审批通过 → **立即可见**；驳回则带原因、可改后重提。
3. 班长录入课程 → **直接可见**（免审）。
4. 同学把"已通过"的作业改内容 → 状态**回到待审**，公开列表暂时隐藏（防绕过）。
5. 我的课表有课 + 该课有作业 3 天内截止 + 我未提交 → 首页**红色条幅出现**；点"已提交" → **条幅消失**。
6. 匿名评论：同学端看到"匿名同学 + 黑客头像"且拿不到真实 ID；管理员可查到真实用户。
7. 除白名单外，无 token 访问一律 401。
8. **（v3）课表截图导入**：上传课表截图 → AI 识别出课程条目 → 核对修改 → 确认导入 → 课表立即出现；勾选"申请入库"的课程在"我的申请"可见。
9. **（v3）七天免登录**：登录后 7 天内任意访问不要求重新登录；token 剩余不足 3 天时任意请求自动续签（浏览器存储无感更新）。
10. **（v3）作息与周数**：设置第一周开始日期后课表显示「第 N 周」并可翻页查看任意周；非视图周课程虚化；修改某节上课时间后下课时间与后续节次自动顺延。
11. **（v3）调课**：拖拽课程到新位置 → 选"临时"仅本周生效（下周自动恢复）或"永久"本学期变更。
12. **（v3）校园日历**：节假日/考试/竞赛事件出现在课表「大学活动」行与首页日历卡；数据更新后登录可见提示。
