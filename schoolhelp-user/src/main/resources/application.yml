server:
  port: 8101

spring:
  application:
    name: schoolhelp-user
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: "ENC(VcDbRCV00FBG3gaLdj/SqHPXA5j+J8lEZ21+2FeS51n5yb/argkKZI0SViiUxEsM)"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/schoolhelpdb?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: sgtxgx
    password: "ENC(eHYf67sDEjnzvSJvHTnLHTJbM06xh704LsbtxRgk7NgJ/gu94Ff0rIOsEHh2PqEA)"
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 文件上传目录（开发期本地；部署改 NAS /mnt/nas/uploads）
schoolhelp:
  file:
    upload-dir: ${SCHOOLHELP_UPLOAD_DIR:D:/homework/schoolHelp/schoolHelp/schoolhelp-user/uploads}
    base-url: /files
  # AI 问答（免费大模型 OpenAI 兼容接口，默认智谱 GLM-4-Flash；API Key 未配置时功能不可用）
  # API Key 以 Jasypt ENC 加密存储（仓库公开，不落明文），口令经环境变量 JASYPT_ENCRYPTOR_PASSWORD 注入
  ai:
    base-url: ${SCHOOLHELP_AI_BASE_URL:https://open.bigmodel.cn/api/paas/v4}
    api-key: ${SCHOOLHELP_AI_API_KEY:}
    model: ${SCHOOLHELP_AI_MODEL:glm-4-flash}
    vision-model: ${SCHOOLHELP_AI_VISION_MODEL:glm-4v-flash}

# ===== 配置加密（Jasypt）=====
# 数据库/Nacos 等敏感字段以 ENC(密文) 形式存储；解密口令通过环境变量注入，不落盘、不进仓库。
# 本地运行：先设置环境变量 JASYPT_ENCRYPTOR_PASSWORD（start-all.ps1 会自动从 .env.local 加载）。
jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
    algorithm: PBEWITHHMACSHA512ANDAES_256
    key-obtention-iterations: 1000
    pool-size: 1
    salt-generator-classname: org.jasypt.salt.RandomSaltGenerator
    iv-generator-classname: org.jasypt.iv.RandomIvGenerator
    string-output-type: base64
