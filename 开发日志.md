# schoolHelp 开发日志

> 记录项目从立项到现在的关键决策、踩坑与迭代。按时间倒序更实用，本文按**主题分阶段**组织，每段标注时间段。
> 项目定位：哈尔滨学院专属校园助手，研一练手项目。本地路径 `D:\homework\schoolHelp\schoolHelp`。

---

## 阶段 0 · 立项与定位（2026-08 中旬 ~ 08-下旬）

**起点**：想做一个"自己班/自己专业能用起来"的小工具，练手微服务 + Vue 全栈，而不是又一个堆在 GitHub 的 demo。

**需求收敛为三条主线**：
1. 课表（能看 + 能自己录）
2. 课程库与作业（统一信息源，截止提醒）
3. 资料与评论（同学之间共享、吐槽）

**定位红线（当时就定了）**：
- 不预填任何真实人名；
- 面向小范围、不赚钱、纯练手；
- 先审后展示，可删可溯源。

> 这条红线后来在"合规咨询"阶段被反复验证是对的。

---

## 阶段 1 · 技术选型与架构定型（08 下旬）

**定稿技术栈**：
- Java 17 + Spring Boot 3.2.4
- Spring Cloud 2023.0.1 + Spring Cloud Alibaba 2023.0.1.0（Nacos 注册中心）
- Spring Cloud Gateway（统一入口 + JWT 校验）
- OpenFeign（服务间调用）
- MyBatis-Plus 3.5.7
- MySQL 8（库 `schoolhelpdb`）
- JWT（Hutool 签发）+ BCrypt
- 前端 Vue3 + Vite + Element Plus + Pinia + Vue Router + axios（移动端 + PC 端双份）

**工程结构定型**：父 POM 聚合 5 模块 —— common / gateway(8080) / user(8101) / course(8102) / biz(8103)，全部注册 Nacos。

**关键设计决定**：
- **gateway 是唯一对外入口**，`/api` 前缀经 `StripPrefix` 后转发；
- **UserContext** 从网关注入的请求头里取 userId/role，业务代码不解析 token；
- **common 模块**收敛 Result / 异常 / 常量 / JwtUtil，避免各服务重复实现；
- **biz 模块**专职"聚合类"业务（跨服务），course/user 各管各的数据。

---

## 阶段 2 · v1 后端跑通（08 下旬）

**成果**：端到端冒烟通过。

链路：注册 → 登录签发 JWT → 带 token 访问 200 / 无 token 401；建课 → 发作业 → 课表录入 → 临期条幅正确过滤 → 标记提交后条幅移除；Feign 跨服务调用正常。

**踩坑与修复**：
1. **gateway → user 503**：曾一度是唯一阻塞项，最后定位为解决（服务注册/路由配置问题）。
2. **列表接口 500**：排查后实为 **405**，路径写错了——正确接口是 `GET /course/course/list`（模块 `${spring.application.name}` + `@RequestMapping("/course")` 叠加）。
3. **Vite 代理 502**：**假警报**。根因是 `.NET HttpClient / curl` 带 `Expect: 100-continue` 触发 Netty 解析 bug；**真实浏览器的 fetch/XHR 不发这个头，不受影响**。为兜底在 `vite.config.js` 里 `removeHeader('Expect')`。

**中文乱码 `????` 事件（重要教训）**：
- 现象：库里/控制台中文变问号。
- 排查：**不是代码/JDBC 问题**，是 09-07 某次用编码错误的工具写入造成的**源头脏数据**（HEX 证据：`3F3F3F3F` = 四个问号）。
- 判据确立：**必须用 `SELECT HEX(字段)` 查库**，控制台 GBK 乱码是假象，不能作为判断依据。
- 处置：清理脏数据后重建。

---

## 阶段 3 · v2 审批流改造（09 初）

**动因**：同学能自由创建课程/作业/资料，内容不可控。要"先审后展示"。

**落地内容**：`course` / `assignment` / `course_material` 三表统一加审批字段：`apply_user_id` / `apply_note` / `review_note` / `reviewed_at` / `status`。

**状态机**：
```
0 待审批 → 1 已通过（公开可见）
         → 2 已驳回（可改后重提）
课程另有 3 下架
```

**权限规则（v2 定稿）**：
- 全员可申请；**班长/管理员录入直接通过**（校园现实：他们本就是内容维护者）；**普通同学走审批**；
- 仅管理员可审批，**驳回必须填原因**；
- 公开列表只显示 `status=1`；
- 迁移脚本 `v2_approval_migration.sql` 对存量数据回填：老数据视为已通过。

**验证**：审批全链路通过——学生申请 → 待审不出现在公开列表 → 我的申请可见 → 管理员审批 → 通过立即可见 / 驳回带原因 → 驳回无原因返 400 → 可删自己待审申请。

---

## 阶段 4 · 前端双端开发（09 上旬）

**成果**：移动端 `schoolhelp-web`（Vite 5173）+ PC 端 `schoolhelp-web-pc`（Vite 5174），两端构建均成功。

**页面清单**：
- 移动端：Login / Register / Schedule(支付宝风格周视图) / Courses / Assignments / Mine / CourseDetail / CourseForm / AssignmentForm / ProfileEdit / PasswordChange / MyApplications / AdminReview + MainLayout。
- PC 端：PC 独有 Home（首页推荐网格）/ PcLayout；其余 9 页 PC 化；**PC 无 Mine，登录跳 `/`**；**移动端无 Home，登录跳 `/schedule`**。

**PC `/assignments` 重写**：从"按课选"改为**课程库全部作业 + 课程筛选下拉**，实测正常（3 课程分组、倒计时、临期条幅）。

**踩坑**：
- **课表页空白**：`Schedule.vue` 漏 `import { reactive }` → setup ReferenceError，已修。
- **Element Plus el-select / el-dropdown 自动化点击不可靠**：popper 是 teleport 渲染，属**自动化工具问题，非应用 bug**。

---

## 阶段 5 · 天气功能（09 上旬）

**成果**：双端实测通过。

- 共享组件 `WeatherPanel.vue`（两端同代码，约 13KB）；
- 数据源 **Open-Meteo 免费 API，无需 key**，直连；
- 坐标写死哈尔滨学院 `45.716,126.59`，时区 Asia/Shanghai；取 current + daily 3 天；
- **穿衣推荐**：按体感温度分层 + 天气码 + 风速修正；
- **弹窗策略**：sessionStorage 会话只弹一次，localStorage 按天"今日不再提示"；
- **图标**：用 **emoji 字符串**渲染（弃 el-icon，因为缺独立的 Rain/Snow 图标）；
- **嵌入点**：移动端 Schedule.vue 天气按钮 + 挂载自动弹；PC Home.vue 日期右侧按钮。

**遗留待办**：用户后续自行申请高德 key，替换坐标/地理能力。

---

## 阶段 6 · emoji 乱码核查（09-08 ~ 09-09）

**现象**：PowerShell 里看 Home.vue / Login.vue，疑似 emoji 变 `??`。

**结论**：用 node 以 UTF-8 读文件验证（脚本 `pc-emoji-check.cjs`）—— Home.vue / Login.vue 均 `emojiPairs=1`、`literal??=0`，`👋` 是**真实 emoji**，文件未损坏。**PowerShell 控制台的 `??` 是编码假象，无需修复。**

**教训固化**：判断文件是否有编码问题，**要用能正确处理 UTF-8 的工具（node/python）读字节**，别用 PowerShell 控制台肉眼判断。

---

## 阶段 7 · 源码走查与修复（09-09 ~ 09-10）

**范围**：后端 5 模块全部源码 + 两端前端公共层与全部页面 + 数据库 DDL 核对。

**走查结论**：核对 DDL 后确认 `assignment` 表无 `updated_at`（course 有），实体与表**一致，非 bug**。

**发现并按严重度处理**：

| 级别 | 问题 | 处置 |
|------|------|------|
| 高 H1 | `CourseService.update` / `AssignmentService.update` 的校验只查归属不查 `status` → 同学可改自己"已通过"的内容且不重进审批，**绕过审核** | ✅ 已修：非特权角色（普通同学）编辑后一律回到 `status=0` 重新审批；班长/管理员保持原状态 |
| 中 M1 | `biz` 的 `markSubmitted` / `unmark` 不校验 assignmentId 是否存在 → 作业被删后仍可标记，产生脏数据 | ✅ 已修：标记前 Feign 校验作业存在且已通过 |
| 中 M2 | `CourseMaterialController.upload` 收了 `applyNote` 参数，但 `CourseMaterialService.upload` 方法签名无该参数 → 申请说明丢失 | ✅ 已修：打通 controller → service → entity 全链路；两端前端上传时补 `applyNote` |
| 低 L2 | 评论 `add` 不校验 courseId 是否存在 | ✅ 已修：发表前 Feign 校验课程存在且已通过 |
| 低 L3 | `Assignment` 无 `updated_at`，缺审计时间 | ⏸ 记录，暂不改（实体与表一致，改需动 DDL） |
| 低 L4 | `Register.vue` 本地算密码强度，未调 `/auth/password-strength` | ⏸ 记录，体验优化项 |
| 低 L5 | `UrgentAssignmentService.daysLeft` 用 Duration 计算，建议改 `ChronoUnit.DAYS.between` | ⏸ 记录，风格建议 |
| 中 M3 | `RegisterDTO` 密码 6-32 位无字母数字强制，与前端 min 8 + 必须含字母数字**口径不一致**，绕过前端可注册弱密码 | ✅ 已修：后端 DTO 统一为 8-32 位 + 字母数字强校验；前端用户名长度同步为 3-20 |

**澄清（非 bug）**：
- **L1 课表自录课程点击 404**：**不成立**。两端 `onCourseClick` 已判 `item.courseId` 为空则转编辑，无需改。
- **emoji `??`**：见阶段 6，假象。

**编译验证**：改动后 `mvnw compile` **BUILD SUCCESS**（course 14 源文件、biz 13 源文件重编通过）。

---

## 阶段 8 · 配置加密与 Git 预清理（09-10）

**背景**：准备把源码推到 GitHub，`application.yml` 里数据库/Nacos 密码为明文，公开仓库会泄露。

**做法（Jasypt 字段级加密）**：

- 引入 `com.github.ulisesbocchio:jasypt-spring-boot-starter:3.0.5`（在父 POM 定版本，common 模块引依赖，4 个服务传递获得）。
- 4 个服务的 `application.yml` 中 `spring.datasource.password`、`spring.cloud.nacos.discovery.password` 均改为 `ENC(密文)`；新增统一 `jasypt.encryptor` 配置段。
- 密文算法与 jasypt-spring-boot 默认一致：`PBEWITHHMACSHA512ANDAES_256` + `RandomSaltGenerator` + `RandomIvGenerator` + 1000 迭代 + base64（用 jasypt 官方库 API 生成并自检回解）。
- 解密口令 `JASYPT_ENCRYPTOR_PASSWORD` **不落仓库**：本地放 `.env.local`（已被 `.gitignore` 忽略），`start-all.ps1` 自动加载；服务器用环境变量注入。

**实测验证**（关键：社区有 SB3 兼容性争议，必须真跑）：
- 打包后带 `JASYPT_ENCRYPTOR_PASSWORD` 启动，日志出现 `DefaultLazyEncryptor` 初始化 + Nacos 注册成功 + Tomcat 就绪。
- 经网关联调：`/api/user/auth/login`（admin/admin123）→ 200；`/api/course/course/list` → 200（3 条）；`/api/biz/urgent/assignments` → 200（跨服务 Feign）；`/api/biz/comment/course/1` → 200。**证明密文解密后真实连上了 MySQL 与 Nacos**。

**Git 推送预清理**：
- 补齐 `.gitignore`：忽略 `node_modules/`、`dist/`、`logs/`、`uploads/`、`.env.local` 等；新建 PC 端缺失的 `schoolhelp-web-pc/.gitignore`。
- 暂存区从「含两个前端依赖（各 112MB）+ 日志（4×12MB）」瘦身到约 145 文件 / 0.56MB。

**结论**：配置加密已落地并通过端到端验证；源码可安全推送（提醒：JWT 密钥仍硬编码在 `JwtUtil`，建议后续一并外置）。

---

## 待办清单（滚动更新）

1. ⬜ `v2_approval_migration.sql` 在部署环境同步执行；同步更新 `v1_schema.sql`（或补 docs 说明）。
2. ⬜ 用户真机 Edge 强制刷新（Ctrl+F5）验证前端。
3. ⬜ 高德 key 申请后替换天气组件。
4. ⬜ 教师师资主页链接 URL（待用户提供）。
5. ✅ M3 密码策略前后端口径已统一（8-32 位 + 字母数字）。
6. ⬜ L3/L4/L5 低优先级优化（审计字段、强度接口、日期计算风格）。
7. ⬜ 香港区环境搭建 + 上线验证。
8. ⬜（可选）清理 PC 端残留移动样式 —— PC 全局 style.css 已兜住，可跳过。
9. ✅ 配置敏感信息已用 Jasypt 加密（数据库/Nacos 密码）；JWT 密钥硬编码待后续外置到配置/环境变量。

---

## 复用性技术坑备忘

- **PowerShell 5.1 读中文按 GBK 乱码** → `.ps1` 用 UTF-8 BOM；要解析 JSON 用 `node xxx.cjs` 直接读。
- **多行 `eval` 引号被 shell 破坏** → 用 base64 编码脚本再解码执行。
- **mysql.exe 密码含 `!`** → 用单引号包裹；`--host=` / `--password=` 用完整 token 防拆分；stderr 警告致 exit 1 → 用 `-B` batch + `-e` 单行。
- **判断中文/emoji 编码问题** → 用 node/python 读字节，别信控制台。
- **查库中文** → 用 `HEX()`，别信控制台。
