# schoolHelp 代码健壮性走查报告

> 走查时间：2026-09-09 ~ 2026-09-10
> 走查范围：后端 5 模块全部源码 + 两端前端（公共层 + 全部页面）+ 数据库 DDL 核对
> 结论摘要：发现 **1 个高危、3 个中危、5 个低危**；其中 **H1 / M1 / M2 / L2 已修复并通过编译**，其余记录待决策。
> 修复后编译验证：`mvnw -DskipTests compile` → **BUILD SUCCESS**

---

## 一、走查方法

1. **通读源码**：按模块（common → gateway → user → course → biz）逐层读，重点看 Controller 的入参校验、Service 的权限/状态校验、Feign 调用的错误处理。
2. **交叉核对 DDL**：把实体字段与 `sql/v1_schema.sql` + `v2_approval_migration.sql` 逐字段对比，排除"实体表不一致"类伪问题。
3. **核对前端**：两端同名页面对比、API 定义对比、路由与权限守卫。
4. **验证编码类问题**：用 node 以 UTF-8 读字节，排除控制台乱码造成的误判。
5. **改完即编译**：修复后跑 `mvnw compile` 确认不破坏构建。

**已覆盖**：`CourseService` / `AssignmentService` / `CourseMaterialService` 全文、`CourseController` / `AssignmentController` / `CourseMaterialController` / `AuthController`、`AssignmentSubmitService` / `CourseCommentService` / `UrgentAssignmentService`、`CourseFeignClient` / `UserFeignClient`、`Result` / `GlobalExceptionHandler` / `BusinessException` / `CommonConstants`、`RegisterDTO`、`Course` / `Assignment` / `CourseMaterial` 实体、两端 `Schedule.vue` / `CourseDetail.vue` / `AdminReview.vue` / `MyApplications.vue` / `Register.vue` 等。

---

## 二、问题总览

| 编号 | 级别 | 模块 | 问题 | 状态 |
|------|------|------|------|------|
| **H1** | 🔴 高 | course | 编辑已通过内容不校验 status，**可绕过审批** | ✅ 已修复 |
| **M1** | 🟠 中 | biz | 标记已提交不校验作业是否存在 → 脏数据 | ✅ 已修复 |
| **M2** | 🟠 中 | course | `applyNote` 参数链路断裂 → 申请说明丢失 | ✅ 已修复 |
| **M3** | 🟠 中 | user | 密码强度前后端口径不一致 | ✅ 已修复 |
| **L2** | 🟡 低 | biz | 评论不校验 courseId 是否存在 | ✅ 已修复 |
| **L3** | 🟡 低 | course | `assignment` 无 `updated_at`，缺审计时间 | ⏸ 记录（需改 DDL） |
| **L4** | 🟡 低 | 前端 | `Register.vue` 未调用后端强度接口 | ⏸ 记录 |
| **L5** | 🟡 低 | biz | `daysLeft` 日期计算风格 | ⏸ 记录 |
| ~~L1~~ | — | 前端 | 课表自录课程点击 404 | ❌ 不成立（已澄清） |

---

## 三、高危问题（已修复）

### H1 · 编辑已通过内容可绕过审批 🔴

**位置**：
- `schoolhelp-course/.../service/CourseService.java` → `update(...)` / `requireEditable(...)`
- `schoolhelp-course/.../service/AssignmentService.java` → `update(...)` / `checkEditable(...)`

**问题**：`requireEditable` / `checkEditable` 只校验"是不是创建者/申请人"，**不看 `status`**。而 `update` 里 `status` 只在 `== STATUS_REJECTED` 时才重置为待审。

后果：一个普通同学申请课程，管理员审批**通过（status=1）**后，该同学仍可编辑这条已通过的内容（改课程名/简介/作业截止时间等），且状态**保持 1 不变**，**完全绕过审批直接生效**。

**原代码（节选）**：
```java
// CourseService.update
exist.setDescription(course.getDescription());
if (exist.getStatus() == CommonConstants.STATUS_REJECTED) {   // ← 只处理"驳回"这一种
    exist.setStatus(CommonConstants.STATUS_PENDING);
    ...
}
courseMapper.updateById(exist);
```

**修复**（改为非特权角色一律回待审）：
```java
// 非特权角色（普通同学）编辑后一律回到待审批，重新走审批
// （防止同学对已通过(1)的课程改内容绕过审核）；班长/管理员为课程维护者，保持原状态
boolean privileged = role != null && (role == CommonConstants.ROLE_MONITOR || role == CommonConstants.ROLE_ADMIN);
if (!privileged) {
    exist.setStatus(CommonConstants.STATUS_PENDING);
    exist.setReviewNote(null);
    exist.setReviewedAt(null);
}
```
`AssignmentService.update` 作同样处理。

> **口径说明**：`create` 里"班长/管理员"是免审直通（`privileged = monitor || admin`），因此 `update` 也应对齐为"班长/管理员保持状态"，否则会出现"班长建课免审、改课却被自己打回待审"的不对称。

**修复后行为**：
- 普通同学改自己已通过内容 → **状态回到 0 待审批**，公开列表暂时不可见，需管理员重新审批。
- 管理员/班长改 → 保持原状态。

**编译验证**：✅ 通过。

---

## 四、中危问题

### M1 · 标记已提交不校验作业存在 🟠 ✅已修

**位置**：`schoolhelp-biz/.../service/AssignmentSubmitService.java`

**问题**：`markSubmitted` / `unmark` 直接操作 `assignment_submit` 表，**不校验 assignmentId 对应作业是否存在**。作业被删除后，用户仍可对"幽灵作业"打提交标记，产生大量无主脏数据；`unmark` 本身是删除操作、风险较低，但 `markSubmitted` 会**新增**记录。

**修复**：注入 `CourseFeignClient`，标记前先校验作业存在且已通过；为不因下游抖动阻塞正常用户，Feign 异常时记日志放行。
```java
public void markSubmitted(Long assignmentId, Long userId) {
    requireAssignmentExists(assignmentId);   // ← 新增校验
    if (submitted(assignmentId, userId)) return;
    ...
}
private void requireAssignmentExists(Long assignmentId) {
    try {
        Result<Map<String,Object>> res = courseFeignClient.assignmentDetail(assignmentId);
        if (res == null || !res.isSuccess() || res.getData() == null) {
            throw new BusinessException(404, "作业不存在或未通过审批");
        }
    } catch (BusinessException e) { throw e; }
    catch (Exception e) { log.warn("校验作业{}存在性失败，放行标记: {}", assignmentId, e.getMessage()); }
}
```
同时在 `CourseFeignClient` 新增 `GET /assignment/{id}` 的 `assignmentDetail`。

**编译验证**：✅ 通过。

### M2 · `applyNote` 参数链路断裂 🟠 ✅已修

**位置**：
- `schoolhelp-course/.../controller/CourseMaterialController.java` → `upload(...)`
- `schoolhelp-course/.../service/CourseMaterialService.java` → `upload(...)`

**问题**：Controller 接收了 `applyNote` 参数，但 Service 的 `upload` **方法签名里没有这个参数**，导致同学上传资料时填的"申请说明"**静默丢失**——传到 Controller 就到头了，根本没写进数据库。

**修复**：
1. Service 签名加 `String applyNote`，并写入实体：
```java
public Long upload(Long courseId, String title, String applyNote, MultipartFile file, Long userId, Integer role)
...
if (applyNote != null && !applyNote.trim().isEmpty()) {
    m.setApplyNote(applyNote.trim());
}
```
2. Controller 调用处补传 `applyNote`。
3. 前端两端 `CourseDetail.vue` 上传时补 `fd.append('applyNote', ...)`（PC 端原有，移动端补齐）。

`CourseMaterial` 实体本就有 `applyNote` 字段，无需改 DDL。

**编译验证**：✅ 通过。

### M3 · 密码强度前后端口径不一致 🟠 ✅已修

**位置**：`RegisterDTO`（后端）vs `schoolhelp-web*/src/views/Register.vue`（前端）

**问题**：
- 前端：最少 **8** 位，且**必须含字母与数字**；
- 后端 `RegisterDTO`：仅 **6-32 位**，**无字母数字强制**。

后果：绕过前端（直接打接口）可注册 `123456` 这类弱密码。

**建议（二选一，待用户决策）**：
- 方案 A：后端 `RegisterDTO` 加 `@Pattern`，与前端对齐（8 位 + 字母数字）；
- 方案 B：统一为标准强度校验并复用 `PasswordStrengthUtil`。

> 未擅自改动：属"策略口径"决策，且可能影响既有测试账号。

**修复（采用方案 A，后端对齐前端口径）**：
```java
// RegisterDTO / ChangePasswordDTO 统一为：
@Size(min = 8, max = 32, message = "密码长度需8-32位")
@Pattern(regexp = "^(?=.*[A-Za-z])(?=.*\\d).+$", message = "密码需同时包含字母和数字")
```
同时把前端 `Register.vue` 的用户名长度由 3-30 改为 3-20，与后端 `@Size(min=3,max=20)` 对齐。

> 影响提示：`AdminInitializer` 的默认号 `admin/admin123` 是**程序内部直接落库**、不走注册 DTO，不受新规则影响，仍能创建。

---

## 五、低危问题

### L2 · 评论不校验 courseId 存在 🟡 ✅已修

**位置**：`schoolhelp-biz/.../service/CourseCommentService.java` → `add(...)`

**问题**：直接插入评论，不校验课程是否存在/已通过 → 可对不存在课程产生脏评论。

**修复**：发表前 Feign 校验课程存在且已通过（同样对下游抖动放行）。
```java
try {
    Result<Map<String,Object>> res = courseFeignClient.courseDetail(courseId);
    if (res == null || !res.isSuccess() || res.getData() == null) {
        throw new BusinessException(404, "课程不存在或未通过审批");
    }
} catch (BusinessException e) { throw e; }
catch (Exception e) { log.warn("校验课程{}存在性失败，放行评论: {}", courseId, e.getMessage()); }
```
**编译验证**：✅ 通过。

### L3 · `assignment` 表无 `updated_at` 🟡 ⏸记录

**核对结论**：`course` 表有 `created_at` + `updated_at`，`assignment` 表**只有 `created_at`**；**实体 `Assignment.java` 也只映射了 `createdAt`，与表一致 → 不是"字段缺失"类 bug，而是设计取舍**。影响：作业修改无审计时间。如需，加列即可（本次未改，避免动 DDL）。

### L4 · `Register.vue` 未调用后端强度接口 🟡 ⏸记录

前端在本地计算密码强度，而 common 里已有 `PasswordStrengthUtil` 与对应接口，存在**重复实现、口径漂移**风险。属体验/一致性优化，非功能缺陷。

### L5 · `daysLeft` 日期计算风格 🟡 ⏸记录

`UrgentAssignmentService.daysBetween` 用 `Duration.toDays() + 小时进位` 计算"剩余天数"。逻辑可用，但**语义等价于** `ChronoUnit.DAYS.between(...)` 的推广；建议统一用 `ChronoUnit` 提升可读性（含"当天算 1 天"的进位意图应加注释）。

---

## 六、已澄清（非缺陷）

### L1 · 课表自录课程点击跳 404 ❌ 不成立

**初判**：担心 `Schedule.vue` 点击纯自录条目（无 `courseId`）会跳 `/course/xxx` 导致 404。

**核对结果**：两端 `onCourseClick(item)` **已判空**：
```js
function onCourseClick(item) {
  if (item.courseId) { router.push(`/course/${item.courseId}`) }
  else { editItem(item) }   // 纯手动录入 → 编辑
}
```
**无需修复**。

### emoji `??` 乱码 ❌ 假象

**初判**：PowerShell 里看到 `Home.vue` / `Login.vue` 出现 `??`。

**核对结果**：用 node 以 UTF-8 读文件，`Home.vue` / `Login.vue` 的 `👋` 等均为**真实 emoji**（`emojiPairs>0`、`literal??=0`），文件未损坏。**PowerShell 控制台的 `??` 是终端编码假象**。

**教训**：判断编码问题**用 node/python 读字节**，别信控制台肉眼。

### `assignment` 表缺 `updated_at` ❌ 非 bug

见 L3——实体与表一致。

---

## 七、核查无问题的部分（通过）

| 项 | 结论 |
|----|------|
| 网关白名单 / JWT 透传 / StripPrefix | ✅ 逻辑正确；`AuthGlobalFilter` order=-100 先于路由执行 |
| Feign 直连 Nacos | ✅ 服务间调用不经网关，避免重复鉴权；所需 userId 显式传参 |
| `Result` / `GlobalExceptionHandler` / `BusinessException` 链 | ✅ 统一响应；业务码 400/403/404 与 HTTP 语义对应；兜底 500 不泄露堆栈 |
| 前端 `request.js` 401 / 503 拦截 | ✅ token 失效跳登录、网关不可用有提示 |
| 审批"驳回必填原因" | ✅ 后端 Service 强校验 + 前端表单校验，双重 |
| 匿名评论 userId 屏蔽 | ✅ 非管理员列表接口把 `userId` 置 `null`，管理员走独立接口 |
| 删除权限 | ✅ 管理员任意；普通用户仅能删自己"未通过"的内容；评论为逻辑删除 |
| 核心业务链路（注册→登录→建课→发作业→临期聚合→提交标记） | ✅ 端到端冒烟通过 |
| 数据库字符集 | ✅ utf8mb4 / utf8mb4_unicode_ci |

---

## 八、修复清单（本次落地）

| 文件 | 改动 |
|------|------|
| `CourseService.java` | H1：`update` 非特权角色回待审 |
| `AssignmentService.java` | H1：`update` 非特权角色回待审 |
| `CourseMaterialService.java` | M2：`upload` 加 `applyNote` 参数并落库 |
| `CourseMaterialController.java` | M2：调用处补传 `applyNote` |
| `CourseFeignClient.java` | M1/L2：新增 `assignmentDetail` 接口 |
| `AssignmentSubmitService.java` | M1：标记前校验作业存在 |
| `CourseCommentService.java` | L2：评论前校验课程存在 |
| `schoolhelp-web/src/views/CourseDetail.vue` | M2：移动端上传补 `applyNote` |
| `RegisterDTO.java` | M3：密码 8-32 位 + 字母数字强校验 |
| `ChangePasswordDTO.java` | M3：同上 |
| `AuthController.java` / `AuthService.java` | M3：注释同步为“已强制” |
| 两端 `Register.vue` | M3：用户名长度 3-30 → 3-20，与后端对齐 |

**编译验证**：`mvnw -DskipTests compile` → **BUILD SUCCESS**（common/gateway/user/course/biz 全绿）。

### 追加：配置敏感信息加密（09-10）

应「推 GitHub 不想暴露密码」的需求，把配置里的明文口令改为 Jasypt 密文：

| 文件 | 改动 |
|------|------|
| 父 `pom.xml` / `schoolhelp-common/pom.xml` | 引入 `jasypt-spring-boot-starter:3.0.5`（版本在父 POM 统一管理） |
| 4 个 `application.yml`（user/course/biz/gateway） | `datasource.password`、`nacos.discovery.password` → `ENC(密文)`；新增 `jasypt.encryptor` 配置段 |
| `start-all.ps1` | 启动前从 `.env.local` 加载 `JASYPT_ENCRYPTOR_PASSWORD` |
| `.gitignore` | 补充忽略 `.env.local` 等密钥文件 |

**验证**：带解密口令启动，4 服务全部就绪；经网关联调登录/课程/临期作业/评论接口均 200 → 密文解密后真实连上 MySQL 与 Nacos。

---

## 九、后续建议（未落地）

1. ~~**M3 密码策略统一**（前后端一致，推荐 8 位 + 字母数字）。~~ ✅ 已完成。
2. **历史弱密码用户**：新规则只对**新注册/改密**生效，既有老账号密码不会自动升级；如需可强制或引导用户改密。
3. **生产环境密钥外置**：`application.yml` 的 DB 密码、Nacos 密码**已用 Jasypt 加密**（✅ 09-10 完成）；剩下的 `JwtUtil.SECRET` 仍为硬编码，建议后续一并外置到配置/环境变量。
4. **L3 作业审计字段**：如业务需要，给 `assignment` 加 `updated_at`。
3. **L4 复用强度接口**：`Register.vue` 改调 `/auth/password-strength`。
4. **L5 日期计算**：统一 `ChronoUnit`。
5. **批量作业接口**：`UrgentAssignmentService` 目前按课程串行拉作业，课程量大时建议加"按课程 ID 批量查作业"接口。
6. **评论治理**（可选）：违规词过滤 + 频率限制 + `robots.txt` 禁收录（面向公网时的防滥用）。
7. **PC 端残留移动样式清理**（可选）：当前 PC 全局 `style.css` 已兜住，可跳过。
